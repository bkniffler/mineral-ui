/* @flow */
import styled from '@emotion/styled';
import _StartEnd from '../../../../../library/StartEnd';
import { containerStyles } from '../../Flex/common/DemoFlex';

export default styled(_StartEnd)((props) => ({
  ...containerStyles(props)
}));
