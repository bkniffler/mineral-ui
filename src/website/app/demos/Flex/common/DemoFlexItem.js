/* @flow */
import styled from '@emotion/styled';
import { FlexItem as _FlexItem } from '../../../../../library/Flex';
import { boxStyles } from '../../Box/common/DemoBox';

export default styled(_FlexItem)((props) => boxStyles(props));
